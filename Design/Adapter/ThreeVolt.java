package Adapter;

// Adapter interface for 3 volt
public interface ThreeVolt {
    // Uses the same functions as that of adaptee class (which needs to be changed
    // as per use)
    public int getCurrent();
}
